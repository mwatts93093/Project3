            // DEBUG STATEMENT TO FIND WHY NO WORKY
            // Based on debug output, not working because jobs get de-queued immediately to run rather than show in the queue.
            // Changing to have list show active jobs as well.
            for (int i = 0; i < job_count; i++) {
                printf("DEBUG: job_queue[%d] = { name: %s, exec_time: %d, priority: %d }\n",
                       i, job_queue[i].name, job_queue[i].execution_time, job_queue[i].priority);
            }